fastapi==0.115.0
mangum==0.19.0
pydantic==2.10.6
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
redis==5.2.1
# boto3 is pre-installed in the Lambda runtime but must be explicit for Docker
boto3==1.35.0
# requests is used by scripts/backfill.py (run inside this container via docker compose exec)
requests==2.32.3
